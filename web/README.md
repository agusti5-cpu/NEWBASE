# NEWBASE web

Public results dashboard. It reads `data/latest-trade-run.json`, shows only accepted opportunities, and refreshes automatically every 60 seconds.

Deployment can be connected to the repository host separately; the UI is intentionally static and requires no paid service.